[Unit]
Description=Persistent Tmux Session Daemon for Antigravity AI
After=network.target

[Service]
Type=forking
ExecStart=%h/.local/bin/start-tfm-daemon
ExecStop=/usr/bin/tmux kill-session -t tfm
Restart=on-failure

[Install]
WantedBy=default.target
